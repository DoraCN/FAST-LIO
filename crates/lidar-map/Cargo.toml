[package]
name = "lidar-map"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Occupancy / voxel map built incrementally from FAST-LIO point clouds"
repository.workspace = true
readme = "README.md"

[dependencies]
image = "0.25"
