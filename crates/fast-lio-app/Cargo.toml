[package]
name = "fast-lio-app"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Offline driver application for FAST-LIO (not published to crates.io)"
publish = false

[features]
default = ["livox-sdk2"]
livox-sdk2 = ["fast-lio-driver/livox-sdk2"]

[dependencies]
fast-lio = { path = "../fast-lio", version = "0.1" }
fast-lio-driver = { path = "../fast-lio-driver", version = "0.1", features = ["livox-sdk2"] }
ctrlc = "3"
